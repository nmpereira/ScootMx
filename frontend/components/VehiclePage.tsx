import { View, Text } from "react-native";
import React from "react";
import { ScooterCardProps } from "@/components/ScooterCard";

const VehiclePage = (props: ScooterCardProps) => {
  return (
    <View>
      <Text>VehiclePage</Text>
    </View>
  );
};

export default VehiclePage;
